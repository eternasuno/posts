name: Deploy to vercel

on:
    push:
        branches: [ "main" ]

jobs:
    deploy:
        runs-on: ubuntu-latest
        steps:
            - name: deploy
              run: |
                curl ${{ secrets.VERCEL_TOKEN }}
        
        
